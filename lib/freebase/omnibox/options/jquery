$( function() {
    var $input = $('input[name="server"]')
    $input.val( localStorage.serverURI )

    // Saves options to localStorage.
    function save_options() {
        localStorage.serverURI = $input.val()
    }
} )

