( function() {
    mimis.load( '.../lib/xregexp/js' )

    return {
        get stmt() { return con },
        exec : function( sql, env ) {
            console.log( 'db.exec:' + sql )
            return {
                each : function( f ) {
                }
            }
        },
    }
} )()
