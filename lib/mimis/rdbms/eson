( function() {
return {
    get stmt() { return con },
    exec : function( sql, env ) {
        console.log( 'db.exec' )
        return {
            each : function( f ) {
            }
        }
    }
} )()
