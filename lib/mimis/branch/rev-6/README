If mvn doesn't work:
  * make -C .../lib/apache/maven/

Available targets:
  * mvn package exec:exec -D (spider|chatbot|jetty:run)
  * mvn jar:jar

Running:
  pullhash .../lib/neo4j/jar
  pullhash .../lib/java/plugin/plugin.jar
  ant init-ivy jar
  
