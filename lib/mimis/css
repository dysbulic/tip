#filelist {
    border: 1px solid;
}

#filelist:hover {
    background-color: #533;
}

#files {
    overflow: auto;
}

html, body, .container, .span4, .span8, #files, #filelist, .row {
    height: 100%;
}

input, label {
    display: block;
}

label:hover, label:active, input:hover, input:hover + label, input:active + label,
.selected, .selected + label {
    background-color: gray ! important;
}
