( function() {
    var insPoint = document.documentElement.lastChild // Will fail when nested
    for( idx in var script = [ '.../lib/jquery/js', '.../lib/mimis/jquery', 'mimis' ] ) {
        console.dir( idx, script )
    }
} )()
