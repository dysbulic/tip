$( function() {
    //mimis.$root.viewBox = mimis.$root.bbox

    function pullChildren( $root ) {
        var $children =
            $root.children( '[inkscape:groupmode="layer"]' )
        console.log( $children.size() )
        if( $children.size() > 0 ) {
            var $list = $('<ol/>')
            
        }
    }
    pullChildren( mimis.$root )
} )
