html, input, textarea {
  font-family: Helvetica;
  font-size: 13pt;
  background-color: rgb(252,250,245);
  text-align: center;
  padding: 4em;
}

fieldset ul {
  display: table;
}

fieldset li {
  display: table-row;
}

fieldset label, fieldset input {
  display: table-cell;
}

legend {
  font-size: 125%;
}

fieldset label {
  text-align: right;
  padding-right: .2em;
  margin-top: .4em;
}

label:after {
  content: ':';
}

form ul {
  padding: 0;
  margin: 0;
  list-style: none;
}

/* input[type='submit'] li:first-parent { */

label[for="submit"] {
  visibility: hidden;
}

input[type='submit'] {
  float: right;
  margin-right: 2em;
}

input[type='text'], input[type='password'] {
  width: 17em;
}

input[type='text'], input[type='password'], textarea {
  padding: .25em .5em;
}

fieldset {
  border-radius: 4px;
  margin: auto;
  padding: .5em;
  display: inline-block;
}

fieldset, input {
  text-align: left;
}
