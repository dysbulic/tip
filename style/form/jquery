$( function() {
    $('fieldset').addClass( 'ui-widget-content' )
    
    $('input:text, input:password, textarea')
        .addClass( 'ui-state-highlight ui-corner-all' )

    $( 'button, input:submit' ).button()

    // ToDo: Add appearance change for not saved changed elements
} )
