var __ = {
  add :
    function(type, id) {
      if('type' == 'match') {
        return {
          match:
            function(string) {
                $(/(\(\?P<(\w+))/)/).each(function(match) { alert(match); });
                             }
        return function() {};
                       }
         };
